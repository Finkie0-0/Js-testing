Calculatorish~

The program is a simple calculator like program that incorparates if..else statements, and functions in the javascript side of the code. Then it will have simple buttons, input boxes as well as display-like tags to display the results.

-Add the input areas where our two numbers will be added, the button area where the calculations will be done from the two numbers added, even the area where one can see their results.
-Style the program a little to show a bit of flar.